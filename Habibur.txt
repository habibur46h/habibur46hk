<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <title>Video App</title>

  <style>
    html, body {
      margin: 0;
      padding: 0;
      width: 100%;
      height: 100%;
      background: #000;
      overflow: hidden;
      font-family: Arial, sans-serif;
    }

    #splash {
      position: fixed;
      inset: 0;
      background: #000;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      z-index: 10;
    }

    iframe {
      width: 100%;
      height: 100%;
      border: none;
    }
  </style>
</head>
<body>

  <!-- Splash Screen -->
  <div id="splash">Loading video…</div>

  <!-- Video / Ad Page -->
  <iframe
    id="videoFrame"
    src=""
    allow="autoplay; fullscreen; encrypted-media"
    allowfullscreen>
  </iframe>

  <script>
    const targetUrl = "https://www.effectivegatecpm.com/jiuc9ux9j?key=92ce8c258037e8296e9a975f2f9dfeb6";

    // Delay for splash screen (helps ad compatibility)
    setTimeout(() => {
      document.getElementById("videoFrame").src = targetUrl;
      document.getElementById("splash").style.display = "none";
    }, 1500);

    // Disable back navigation (basic)
    history.pushState(null, null, location.href);
    window.onpopstate = function () {
      history.go(1);
    };
  </script>

</body>
</html>
